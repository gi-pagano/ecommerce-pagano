import { FaShoppingCart} from "react-icons/fa";

const CartWidget = () => {
    return (
        <FaShoppingCart
        size={30}
        color="white"
        />
    );
};

export default CartWidget;